name: CI
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: ci
          POSTGRES_PASSWORD: ci
          POSTGRES_DB: grainline_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U ci -d grainline_ci"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      # Database — CI uses local Postgres service container, not the Neon pooler/direct URLs from Vercel
      DATABASE_URL: postgresql://ci:ci@localhost:5432/grainline_ci?sslmode=disable
      DIRECT_URL: postgresql://ci:ci@localhost:5432/grainline_ci?sslmode=disable
      # Clerk auth
      CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
      NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
      CLERK_WEBHOOK_SECRET: ${{ secrets.CLERK_WEBHOOK_SECRET }}
      # Stripe
      STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
      NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY }}
      STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
      # Cloudflare R2
      CLOUDFLARE_R2_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_R2_ACCOUNT_ID }}
      CLOUDFLARE_R2_ACCESS_KEY_ID: ${{ secrets.CLOUDFLARE_R2_ACCESS_KEY_ID }}
      CLOUDFLARE_R2_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_SECRET_ACCESS_KEY }}
      CLOUDFLARE_R2_BUCKET_NAME: ${{ secrets.CLOUDFLARE_R2_BUCKET_NAME }}
      CLOUDFLARE_R2_PUBLIC_URL: ${{ secrets.CLOUDFLARE_R2_PUBLIC_URL }}
      R2_PUBLIC_URL: ${{ secrets.R2_PUBLIC_URL }}
      NEXT_PUBLIC_CLOUDFLARE_R2_PUBLIC_URL: ${{ secrets.NEXT_PUBLIC_CLOUDFLARE_R2_PUBLIC_URL }}
      NEXT_PUBLIC_R2_PUBLIC_URL: ${{ secrets.NEXT_PUBLIC_R2_PUBLIC_URL }}
      CLOUDFLARE_R2_PUBLIC_URLS: ${{ secrets.CLOUDFLARE_R2_PUBLIC_URLS }}
      ALLOWED_R2_PUBLIC_URLS: ${{ secrets.ALLOWED_R2_PUBLIC_URLS }}
      UPLOAD_VERIFICATION_SECRET: ci-upload-verification-secret
      # Upstash Redis
      UPSTASH_REDIS_REST_URL: ${{ secrets.UPSTASH_REDIS_REST_URL }}
      UPSTASH_REDIS_REST_TOKEN: ${{ secrets.UPSTASH_REDIS_REST_TOKEN }}
      # Shipping
      SHIPPO_API_KEY: ${{ secrets.SHIPPO_API_KEY }}
      SHIPPING_RATE_SECRET: ${{ secrets.SHIPPING_RATE_SECRET }}
      # Email
      RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
      RESEND_WEBHOOK_SECRET: ${{ secrets.RESEND_WEBHOOK_SECRET }}
      EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
      UNSUBSCRIBE_SECRET: ${{ secrets.UNSUBSCRIBE_SECRET }}
      EMAIL_UNSUBSCRIBE_SECRET: ${{ secrets.EMAIL_UNSUBSCRIBE_SECRET }}
      # AI
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      # Admin / cron
      ADMIN_PIN: ${{ secrets.ADMIN_PIN }}
      ADMIN_PIN_COOKIE_SECRET: ${{ secrets.ADMIN_PIN_COOKIE_SECRET }}
      CRON_SECRET: ${{ secrets.CRON_SECRET }}
      HEALTH_CHECK_TOKEN: ci-health-check-token
      EMAIL_OUTBOX_DAILY_LIMIT: "100"
      # Sentry / app
      SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
      SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
      NEXT_PUBLIC_SENTRY_DSN: ${{ secrets.NEXT_PUBLIC_SENTRY_DSN }}
      NEXT_PUBLIC_APP_URL: ${{ secrets.NEXT_PUBLIC_APP_URL }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npx prisma generate
      - name: Verify Order payment signed authority release
        run: npm run audit:order-payment-signed-authority-release
      - name: Isolate Order payment signed authority until sealed predecessors pass
        run: >-
          mv
          prisma/migrations/20260824030000_prepare_order_payment_signed_authority
          "$RUNNER_TEMP/order-payment-signed-authority-release"
      - name: Verify Order refund record authority release
        run: npm run audit:order-refund-record-authority-release
      - name: Isolate Order refund record authority until sealed predecessors pass
        run: >-
          mv
          prisma/migrations/20260824020000_prepare_order_refund_record_authority
          "$RUNNER_TEMP/order-refund-record-authority-release"
      - name: Verify Order refund claim generation release
        run: npm run audit:order-refund-claim-generation-release
      - name: Isolate Order refund claim generation until sealed predecessors pass
        run: >-
          mv
          prisma/migrations/20260824010000_prepare_order_refund_claim_generation
          "$RUNNER_TEMP/order-refund-claim-generation-release"
      - name: Verify SellerPayoutEvent FORCE migration tree
        env:
          SAVED_SEARCH_RLS_DEPLOY_PHASE: seller-payout-event-force-reviewed
        run: npm run verify:rls-release-artifact
      - name: Verify SellerPayoutEvent FORCE release
        run: npm run audit:rls-seller-payout-event-force-release
      - name: Isolate SellerPayoutEvent FORCE until Phase A passes
        run: >-
          mv
          prisma/migrations/20260823220000_force_seller_payout_event_rls
          "$RUNNER_TEMP/seller-payout-event-force-release"
      - name: Verify SellerPayoutEvent activation migration tree
        env:
          SAVED_SEARCH_RLS_DEPLOY_PHASE: seller-payout-event-activation-reviewed
        run: npm run verify:rls-release-artifact
      - name: Verify SellerPayoutEvent activation release
        run: npm run audit:rls-seller-payout-event-activation-release
      - name: Isolate SellerPayoutEvent activation until authority proofs pass
        run: >-
          mv
          prisma/migrations/20260822180000_enable_seller_payout_event_rls
          "$RUNNER_TEMP/seller-payout-event-activation-release"
      - name: Isolate SellerPayoutEvent authority until all sealed predecessors pass
        run: >-
          mv
          prisma/migrations/20260815210000_prepare_seller_payout_event_authority
          "$RUNNER_TEMP/seller-payout-event-authority-release"
      - name: Verify CheckoutStockReservation FORCE migration tree
        env:
          SAVED_SEARCH_RLS_DEPLOY_PHASE: checkout-stock-reservation-force-reviewed
        run: npm run verify:rls-release-artifact
      - name: Verify CheckoutStockReservation FORCE release
        run: npm run audit:rls-checkout-stock-reservation-force-release
      - name: Isolate CheckoutStockReservation FORCE until Phase A passes
        run: >-
          mv
          prisma/migrations/20260815060001_force_checkout_stock_reservation_rls
          "$RUNNER_TEMP/checkout-stock-reservation-force-release"
      - name: Verify CheckoutStockReservation activation migration tree
        env:
          SAVED_SEARCH_RLS_DEPLOY_PHASE: checkout-stock-reservation-activation-reviewed
        run: npm run verify:rls-release-artifact
      - name: Verify CheckoutStockReservation activation release
        run: npm run audit:rls-checkout-stock-reservation-activation-release
      - name: Isolate CheckoutStockReservation activation until source consistency passes
        run: >-
          mv
          prisma/migrations/20260815060000_enable_checkout_stock_reservation_rls
          "$RUNNER_TEMP/checkout-stock-reservation-activation-release"
      - name: Verify CheckoutStockReservation source-consistency migration tree
        env:
          SAVED_SEARCH_RLS_DEPLOY_PHASE: checkout-stock-reservation-source-consistency-reviewed
        run: npm run verify:rls-release-artifact
      - name: Verify CheckoutStockReservation source-consistency release
        run: npm run audit:rls-checkout-stock-reservation-source-consistency-release
      - name: Isolate CheckoutStockReservation source consistency until authority passes
        run: >-
          mv
          prisma/migrations/20260814053000_prepare_checkout_stock_reservation_source_consistency
          "$RUNNER_TEMP/checkout-stock-reservation-source-consistency-release"
      - name: Verify sealed CheckoutStockReservation authority predecessor
        env:
          SAVED_SEARCH_RLS_DEPLOY_PHASE: checkout-stock-reservation-authority-reviewed
        run: npm run verify:rls-release-artifact
      - name: Verify CheckoutStockReservation authority release
        run: npm run audit:rls-checkout-stock-reservation-authority-release
      - name: Verify StripeWebhookEvent FORCE release
        run: npm run audit:rls-stripe-webhook-event-force-sealed-prefix
      - name: Verify Case FORCE proof equivalence
        run: npm run audit:rls-case-force-release
      - name: Verify Case read-mode release bytes
        run: npm run audit:rls-case-read-mode-candidate
      - name: Verify Conversation and Message authority proof equivalence
        run: npm run audit:rls-conversation-message-authority-release
      - name: Verify Conversation and Message activation proof equivalence
        run: npm run audit:rls-conversation-message-activation-release
      - name: Verify Conversation and Message FORCE release artifact
        run: npm run audit:rls-conversation-message-force-release
      - name: Verify Notification activation proof equivalence
        run: npm run audit:rls-notification-activation-release
      - name: Verify Notification FORCE release artifact
        run: npm run audit:rls-notification-force-release
      - name: Verify DirectUpload retirement proof equivalence
        run: npm run audit:rls-direct-upload-retirement-release
      - name: Verify DirectUpload activation proof equivalence
        run: npm run audit:rls-direct-upload-activation-release
      - name: Verify the sealed StripeWebhookEvent Phase-A predecessor
        run: npm run audit:rls-stripe-webhook-event-activation-sealed-prefix
      - name: Prepare CI runtime policy role
        run: psql "$DIRECT_URL" -f scripts/prepare-ci-rls-runtime-role.sql
      - name: Prove runtime-role provisioning refusals exit nonzero
        run: |
          if psql "$DIRECT_URL" -v runtime_role=ci -v migration_role=ci -f scripts/provision-runtime-db-role.sql; then
            echo "runtime-role provisioning unexpectedly accepted the migration owner as runtime"
            exit 1
          fi
      - name: Isolate the exact Case activation until authority proofs pass
        run: >-
          mv
          prisma/migrations/20260804160000_enable_case_rls
          "$RUNNER_TEMP/case-activation-release"
      - name: Isolate the exact Case FORCE release until Phase A passes
        run: >-
          mv
          prisma/migrations/20260804191000_force_case_rls
          "$RUNNER_TEMP/case-force-release"
      - name: Isolate the exact DirectUpload activation until external grants converge
        run: >-
          mv
          prisma/migrations/20260801194000_enable_direct_upload_rls
          "$RUNNER_TEMP/direct-upload-activation-release"
      - name: Isolate the exact StripeWebhookEvent activation until compatibility proofs pass
        run: >-
          mv
          prisma/migrations/20260805060000_enable_stripe_webhook_event_rls
          "$RUNNER_TEMP/stripe-webhook-event-activation-release"
      - name: Isolate the exact StripeWebhookEvent FORCE release until Phase A passes
        run: >-
          mv
          prisma/migrations/20260810172000_force_stripe_webhook_event_rls
          "$RUNNER_TEMP/stripe-webhook-event-force-release"
      - name: Isolate CheckoutStockReservation authority until webhook FORCE passes
        run: >-
          mv
          prisma/migrations/20260810190000_prepare_checkout_stock_reservation_authority
          "$RUNNER_TEMP/checkout-stock-reservation-authority-release"
      - name: Apply compatible migrations to CI Postgres
        run: npx prisma migrate deploy
      - name: Converge pre-activation production-style runtime grants
        run: psql "$DIRECT_URL" -v runtime_role=grainline_app_runtime -v migration_role=ci -f scripts/provision-runtime-db-role.sql
      - name: Converge pre-activation DirectUpload cleanup-worker grants
        run: psql "$DIRECT_URL" -v cleanup_role=grainline_direct_upload_cleanup_v2 -v runtime_role=grainline_app_runtime -v migration_role=ci -f scripts/provision-direct-upload-cleanup-role.sql
      - name: Prove DirectUpload legacy repair in ephemeral PostgreSQL
        run: npm run audit:rls-direct-upload-legacy-repair
        env:
          DIRECT_UPLOAD_LEGACY_REPAIR_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Restore the exact DirectUpload activation release
        run: >-
          mv
          "$RUNNER_TEMP/direct-upload-activation-release"
          prisma/migrations/20260801194000_enable_direct_upload_rls
      - name: Apply migrations to CI Postgres
        run: npx prisma migrate deploy
      - name: Converge production-style runtime grants after migrations
        run: psql "$DIRECT_URL" -v runtime_role=grainline_app_runtime -v migration_role=ci -f scripts/provision-runtime-db-role.sql
      - name: Converge activated DirectUpload cleanup-worker grants
        run: psql "$DIRECT_URL" -v cleanup_role=grainline_direct_upload_cleanup_v2 -v runtime_role=grainline_app_runtime -v migration_role=ci -f scripts/provision-direct-upload-cleanup-role.sql
      - name: Prove participant Case resolution authority in ephemeral PostgreSQL
        run: npm run audit:rls-case-participant-resolution
        env:
          CASE_PARTICIPANT_RESOLUTION_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove buyer Case-open authority in ephemeral PostgreSQL
        run: npm run audit:rls-case-open-authority
        env:
          CASE_OPEN_AUTHORITY_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Case-reply authority in ephemeral PostgreSQL
        run: npm run audit:rls-case-reply-authority
        env:
          CASE_REPLY_AUTHORITY_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Case-message preflight authority in ephemeral PostgreSQL
        run: npm run audit:rls-case-message-preflight
        env:
          CASE_MESSAGE_PREFLIGHT_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Case-message page authority in ephemeral PostgreSQL
        run: npm run audit:rls-case-message-page
        env:
          CASE_MESSAGE_PAGE_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Case recipient-read authority in ephemeral PostgreSQL
        run: npm run audit:rls-case-recipient-read
        env:
          CASE_RECIPIENT_READ_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Case staff queue authority in ephemeral PostgreSQL
        run: npm run audit:rls-case-staff-queue
        env:
          CASE_STAFF_QUEUE_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Case-aware Order authority in ephemeral PostgreSQL
        run: npm run audit:rls-case-order-active
        env:
          CASE_ORDER_ACTIVE_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Case seller aggregate authority in ephemeral PostgreSQL
        run: npm run audit:rls-case-seller-aggregate
        env:
          CASE_SELLER_AGGREGATE_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Case account-export authority in ephemeral PostgreSQL
        run: npm run audit:rls-case-account-export
        env:
          CASE_ACCOUNT_EXPORT_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Case escalation and cron-transition authority in ephemeral PostgreSQL
        run: npm run audit:rls-case-escalation-cron
        env:
          CASE_ESCALATION_CRON_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Case account-deletion authority in ephemeral PostgreSQL
        run: npm run audit:rls-case-account-deletion
        env:
          CASE_ACCOUNT_DELETION_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Case invariant drafts in rollback-only PostgreSQL
        run: npm run audit:rls-case-invariant-drafts
        env:
          CASE_INVARIANT_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Case and CaseMessage legacy inspection SQL in ephemeral PostgreSQL
        run: npm run audit:rls-case-legacy-inspection
        env:
          CASE_LEGACY_INSPECTION_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Order/payment/shipping legacy inspection SQL in ephemeral PostgreSQL
        run: npm run audit:rls-order-payment-shipping-legacy-inspection-postgres
        env:
          ORDER_PAYMENT_SHIPPING_LEGACY_INSPECTION_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove compatible Order seller key in rollback-only PostgreSQL
        run: npm run audit:rls-order-seller-key-compatibility
        env:
          ORDER_SELLER_KEY_COMPATIBILITY_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove compatible Stripe webhook leases in rollback-only PostgreSQL
        run: npm run audit:rls-stripe-webhook-lease-compatibility
        env:
          STRIPE_WEBHOOK_LEASE_COMPATIBILITY_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Stripe webhook maintenance authority in rollback-only PostgreSQL
        run: npm run audit:rls-stripe-webhook-maintenance-postgres
        env:
          STRIPE_WEBHOOK_MAINTENANCE_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove buyer-deletion fixed-lease verifier rollback in PostgreSQL
        run: npm run audit:buyer-deletion-replay-postgres
        env:
          BUYER_DELETION_REPLAY_POSTGRES_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Order/payment/shipping compatible postflight under the runtime role
        run: npm run audit:rls-order-payment-shipping-compatible-postflight
        env:
          ORDER_PAYMENT_SHIPPING_COMPATIBLE_POSTFLIGHT_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove Conversation and Message legacy cleanup in ephemeral PostgreSQL
        run: npm run audit:rls-conversation-message-legacy-cleanup
        env:
          CONVERSATION_MESSAGE_LEGACY_CLEANUP_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove compatible Case production postflight under the runtime role
        run: npm run audit:rls-case-compatible-db-postflight
        env:
          CASE_COMPATIBLE_DB_POSTFLIGHT_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Restore the exact Case activation release
        run: >-
          mv
          "$RUNNER_TEMP/case-activation-release"
          prisma/migrations/20260804160000_enable_case_rls
      - name: Apply the exact Case activation release
        run: npx prisma migrate deploy
      - name: Converge activated Case runtime grants
        run: psql "$DIRECT_URL" -v runtime_role=grainline_app_runtime -v migration_role=ci -f scripts/provision-runtime-db-role.sql
      - name: Verify migration status
        run: npx prisma migrate status
      - name: Audit final runtime grants and RLS catalog
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Prove promoted Case activation under the runtime role
        run: npm run audit:rls-case-activation-postgres
        env:
          CASE_ACTIVATION_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Restore the exact Case FORCE release
        run: >-
          mv
          "$RUNNER_TEMP/case-force-release"
          prisma/migrations/20260804191000_force_case_rls
      - name: Apply the exact Case FORCE release
        run: npx prisma migrate deploy
      - name: Converge FORCE-hardened Case runtime grants
        run: psql "$DIRECT_URL" -v runtime_role=grainline_app_runtime -v migration_role=ci -f scripts/provision-runtime-db-role.sql
      - name: Verify FORCE migration status
        run: npx prisma migrate status
      - name: Audit FORCE-hardened runtime grants and RLS catalog
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Prove FORCE-hardened Case authority under the runtime role
        run: npm run audit:rls-case-force-postgres
        env:
          CASE_FORCE_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Restore the exact StripeWebhookEvent activation release
        run: >-
          mv
          "$RUNNER_TEMP/stripe-webhook-event-activation-release"
          prisma/migrations/20260805060000_enable_stripe_webhook_event_rls
      - name: Apply the exact StripeWebhookEvent activation release
        run: npx prisma migrate deploy
      - name: Converge activated StripeWebhookEvent runtime grants
        run: psql "$DIRECT_URL" -v runtime_role=grainline_app_runtime -v migration_role=ci -f scripts/provision-runtime-db-role.sql
      - name: Audit activated StripeWebhookEvent grants and RLS catalog
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Prove policyless StripeWebhookEvent activation
        run: npm run audit:rls-stripe-webhook-event-activation-postgres
        env:
          STRIPE_WEBHOOK_EVENT_ACTIVATION_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Enable disposable direct runtime login for the activation postflight proof
        run: >-
          psql "$DIRECT_URL"
          -v ON_ERROR_STOP=1
          -c "ALTER ROLE grainline_app_runtime PASSWORD 'grainline-ci-runtime-postflight-only'"
      - name: Prove activation postflight through the actual runtime login
        run: npm run audit:rls-stripe-webhook-event-activation-postflight-postgres
        env:
          STRIPE_WEBHOOK_EVENT_ACTIVATION_POSTFLIGHT_PROOF_DATABASE_URL: postgresql://grainline_app_runtime:grainline-ci-runtime-postflight-only@localhost:5432/grainline_ci?sslmode=disable
      - name: Re-prove Stripe webhook leases behind policyless activation
        run: npm run audit:rls-stripe-webhook-lease-compatibility
        env:
          STRIPE_WEBHOOK_LEASE_COMPATIBILITY_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Re-prove Stripe webhook maintenance behind policyless activation
        run: npm run audit:rls-stripe-webhook-maintenance-postgres
        env:
          STRIPE_WEBHOOK_MAINTENANCE_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove StripeWebhookEvent database-first rollback and restoration
        run: npm run audit:rls-stripe-webhook-event-activation-rollback
        env:
          STRIPE_WEBHOOK_EVENT_ACTIVATION_ROLLBACK_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Re-audit restored StripeWebhookEvent activation posture
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Restore the exact StripeWebhookEvent FORCE release
        run: >-
          mv
          "$RUNNER_TEMP/stripe-webhook-event-force-release"
          prisma/migrations/20260810172000_force_stripe_webhook_event_rls
      - name: Apply the exact StripeWebhookEvent FORCE release
        run: npx prisma migrate deploy
      - name: Verify StripeWebhookEvent FORCE migration status
        run: npx prisma migrate status
      - name: Audit FORCE-hardened StripeWebhookEvent grants and RLS catalog
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Prove FORCE-hardened StripeWebhookEvent authority
        run: npm run audit:rls-stripe-webhook-event-force-postgres
        env:
          STRIPE_WEBHOOK_EVENT_FORCE_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove StripeWebhookEvent FORCE rollback and restoration
        run: npm run audit:rls-stripe-webhook-event-force-rollback
        env:
          STRIPE_WEBHOOK_EVENT_FORCE_ROLLBACK_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Re-audit restored StripeWebhookEvent FORCE posture
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Restore CheckoutStockReservation authority release
        run: >-
          mv
          "$RUNNER_TEMP/checkout-stock-reservation-authority-release"
          prisma/migrations/20260810190000_prepare_checkout_stock_reservation_authority
      - name: Apply CheckoutStockReservation compatible authority
        run: npx prisma migrate deploy
      - name: Converge compatible reservation function grants
        run: psql "$DIRECT_URL" -v runtime_role=grainline_app_runtime -v migration_role=ci -f scripts/provision-runtime-db-role.sql
      - name: Verify CheckoutStockReservation authority migration status
        run: npx prisma migrate status
      - name: Audit compatible reservation grants and RLS catalog
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Restore CheckoutStockReservation source-consistency release
        run: >-
          mv
          "$RUNNER_TEMP/checkout-stock-reservation-source-consistency-release"
          prisma/migrations/20260814053000_prepare_checkout_stock_reservation_source_consistency
      - name: Prove CheckoutStockReservation source successor before application
        run: npm run audit:rls-checkout-stock-reservation-authority-postgres
      - name: Apply CheckoutStockReservation source-consistency authority
        run: npx prisma migrate deploy
      - name: Converge source-consistent reservation function grants
        run: psql "$DIRECT_URL" -v runtime_role=grainline_app_runtime -v migration_role=ci -f scripts/provision-runtime-db-role.sql
      - name: Verify CheckoutStockReservation source-consistency migration status
        run: npx prisma migrate status
      - name: Audit source-consistent reservation grants and RLS catalog
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Prove promoted CheckoutStockReservation source consistency
        run: npm run audit:rls-checkout-stock-reservation-authority-postgres
      - name: Restore CheckoutStockReservation activation release
        run: >-
          mv
          "$RUNNER_TEMP/checkout-stock-reservation-activation-release"
          prisma/migrations/20260815060000_enable_checkout_stock_reservation_rls
      - name: Re-prove the activation predecessor before application
        run: npm run audit:rls-checkout-stock-reservation-authority-postgres
      - name: Apply CheckoutStockReservation policyless activation
        run: npx prisma migrate deploy
      - name: Converge activated reservation runtime grants
        run: psql "$DIRECT_URL" -v runtime_role=grainline_app_runtime -v migration_role=ci -f scripts/provision-runtime-db-role.sql
      - name: Verify CheckoutStockReservation activation migration status
        run: npx prisma migrate status
      - name: Audit activated reservation grants and RLS catalog
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Prove CheckoutStockReservation activation through the actual runtime login
        run: npm run audit:rls-checkout-stock-reservation-activation-postflight-postgres
        env:
          CHECKOUT_STOCK_RESERVATION_ACTIVATION_POSTFLIGHT_PROOF_DATABASE_URL: postgresql://grainline_app_runtime:grainline-ci-runtime-postflight-only@localhost:5432/grainline_ci?sslmode=disable
      - name: Restore CheckoutStockReservation FORCE release
        run: >-
          mv
          "$RUNNER_TEMP/checkout-stock-reservation-force-release"
          prisma/migrations/20260815060001_force_checkout_stock_reservation_rls
      - name: Apply CheckoutStockReservation FORCE hardening
        run: npx prisma migrate deploy
      - name: Verify CheckoutStockReservation FORCE migration status
        run: npx prisma migrate status
      - name: Audit FORCE-hardened reservation grants and RLS catalog
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Prove FORCE-hardened CheckoutStockReservation authority
        run: npm run audit:rls-checkout-stock-reservation-force-postgres
        env:
          CHECKOUT_STOCK_RESERVATION_FORCE_PROOF_DATABASE_URL: postgresql://grainline_app_runtime:grainline-ci-runtime-postflight-only@localhost:5432/grainline_ci?sslmode=disable
      - name: Prove CheckoutStockReservation FORCE rollback and restoration
        run: npm run audit:rls-checkout-stock-reservation-force-rollback
        env:
          CHECKOUT_STOCK_RESERVATION_FORCE_ROLLBACK_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Re-audit restored CheckoutStockReservation FORCE posture
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Restore SellerPayoutEvent compatible authority release
        run: >-
          mv
          "$RUNNER_TEMP/seller-payout-event-authority-release"
          prisma/migrations/20260815210000_prepare_seller_payout_event_authority
      - name: Verify SellerPayoutEvent compatible authority release
        run: npm run audit:rls-seller-payout-event-authority-release
      - name: Apply SellerPayoutEvent compatible authority
        run: npx prisma migrate deploy
      - name: Converge compatible SellerPayoutEvent runtime grants
        run: psql "$DIRECT_URL" -v runtime_role=grainline_app_runtime -v migration_role=ci -f scripts/provision-runtime-db-role.sql
      - name: Verify SellerPayoutEvent authority migration status
        run: npx prisma migrate status
      - name: Audit compatible SellerPayoutEvent grants and RLS catalog
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Prove SellerPayoutEvent authority in real PostgreSQL
        run: npm run audit:rls-seller-payout-event-authority-postgres
        env:
          SELLER_PAYOUT_EVENT_AUTHORITY_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove zero direct SellerPayoutEvent application access
        run: npm run audit:rls-seller-payout-event-zero-direct-access
      - name: Restore SellerPayoutEvent activation release
        run: >-
          mv
          "$RUNNER_TEMP/seller-payout-event-activation-release"
          prisma/migrations/20260822180000_enable_seller_payout_event_rls
      - name: Re-prove the SellerPayoutEvent activation predecessor
        run: npm run audit:rls-seller-payout-event-authority-postgres
        env:
          SELLER_PAYOUT_EVENT_AUTHORITY_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Apply SellerPayoutEvent policyless activation
        run: npx prisma migrate deploy
      - name: Converge activated SellerPayoutEvent runtime grants
        run: psql "$DIRECT_URL" -v runtime_role=grainline_app_runtime -v migration_role=ci -f scripts/provision-runtime-db-role.sql
      - name: Verify SellerPayoutEvent activation migration status
        run: npx prisma migrate status
      - name: Audit activated SellerPayoutEvent grants and RLS catalog
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Prove SellerPayoutEvent policyless activation through separate logins
        run: npm run audit:rls-seller-payout-event-activation-postgres
        env:
          SELLER_PAYOUT_EVENT_ACTIVATION_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
          SELLER_PAYOUT_EVENT_ACTIVATION_PROOF_RUNTIME_DATABASE_URL: postgresql://grainline_app_runtime:grainline-ci-runtime-postflight-only@localhost:5432/grainline_ci?sslmode=disable
      - name: Prove SellerPayoutEvent activation postflight through the actual runtime login
        run: npm run audit:rls-seller-payout-event-activation-postflight-postgres
        env:
          SELLER_PAYOUT_EVENT_ACTIVATION_POSTFLIGHT_PROOF_DATABASE_URL: postgresql://grainline_app_runtime:grainline-ci-runtime-postflight-only@localhost:5432/grainline_ci?sslmode=disable
      - name: Prove SellerPayoutEvent database-first rollback and restoration
        run: npm run audit:rls-seller-payout-event-activation-rollback
        env:
          SELLER_PAYOUT_EVENT_ACTIVATION_ROLLBACK_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
          SELLER_PAYOUT_EVENT_ACTIVATION_ROLLBACK_PROOF_RUNTIME_DATABASE_URL: postgresql://grainline_app_runtime:grainline-ci-runtime-postflight-only@localhost:5432/grainline_ci?sslmode=disable
      - name: Re-audit restored SellerPayoutEvent activation posture
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Restore SellerPayoutEvent FORCE release
        run: >-
          mv
          "$RUNNER_TEMP/seller-payout-event-force-release"
          prisma/migrations/20260823220000_force_seller_payout_event_rls
      - name: Apply SellerPayoutEvent FORCE hardening
        run: npx prisma migrate deploy
      - name: Verify SellerPayoutEvent FORCE migration status
        run: npx prisma migrate status
      - name: Audit FORCE-hardened SellerPayoutEvent grants and RLS catalog
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Prove FORCE-hardened SellerPayoutEvent authority
        run: npm run audit:rls-seller-payout-event-force-postgres
        env:
          SELLER_PAYOUT_EVENT_FORCE_PROOF_DATABASE_URL: postgresql://grainline_app_runtime:grainline-ci-runtime-postflight-only@localhost:5432/grainline_ci?sslmode=disable
      - name: Prove SellerPayoutEvent FORCE postflight through the actual runtime login
        run: npm run audit:rls-seller-payout-event-force-postflight-postgres
        env:
          SELLER_PAYOUT_EVENT_ACTIVATION_POSTFLIGHT_PROOF_DATABASE_URL: postgresql://grainline_app_runtime:grainline-ci-runtime-postflight-only@localhost:5432/grainline_ci?sslmode=disable
      - name: Prove SellerPayoutEvent FORCE rollback and restoration
        run: npm run audit:rls-seller-payout-event-force-rollback
        env:
          SELLER_PAYOUT_EVENT_FORCE_ROLLBACK_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Re-audit restored SellerPayoutEvent FORCE posture
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Restore Order refund claim generation release
        run: >-
          mv
          "$RUNNER_TEMP/order-refund-claim-generation-release"
          prisma/migrations/20260824010000_prepare_order_refund_claim_generation
      - name: Apply Order refund claim generation preparation
        run: npx prisma migrate deploy
      - name: Verify Order refund claim generation migration status
        run: npx prisma migrate status
      - name: Audit Order refund claim grants and retained RLS posture
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Prove Order refund claims in disposable PostgreSQL
        run: >-
          node --test
          tests/order-refund-claim-generation-migration.test.mjs
          tests/order-refund-claim-generation-postgres.test.mjs
      - name: Restore Order refund record authority release
        run: >-
          mv
          "$RUNNER_TEMP/order-refund-record-authority-release"
          prisma/migrations/20260824020000_prepare_order_refund_record_authority
      - name: Apply Order refund record authority preparation
        run: npx prisma migrate deploy
      - name: Verify Order refund record authority migration status
        run: npx prisma migrate status
      - name: Audit Order refund record grants and retained RLS posture
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Prove atomic Order refund finalization in disposable PostgreSQL
        run: >-
          node --test
          tests/order-refund-record-authority.test.mjs
          tests/order-refund-record-authority-release.test.mjs
          tests/order-refund-record-authority-postgres.test.mjs
      - name: Restore Order payment signed authority release
        run: >-
          mv
          "$RUNNER_TEMP/order-payment-signed-authority-release"
          prisma/migrations/20260824030000_prepare_order_payment_signed_authority
      - name: Apply Order payment signed authority preparation
        run: npx prisma migrate deploy
      - name: Verify Order payment signed authority migration status
        run: npx prisma migrate status
      - name: Audit signed payment grants and retained RLS posture
        run: GRANT_AUDIT_DATABASE_URL="$DIRECT_URL" RUNTIME_DB_ROLE=grainline_app_runtime MIGRATION_DB_ROLE=ci npm run audit:db-grants -- --allow-loopback-ci
      - name: Prove signed Order payment authority in disposable PostgreSQL
        run: >-
          node --test
          tests/order-payment-event-signed-authority-migration.test.mjs
          tests/order-payment-event-signed-authority-postgres-proof.test.mjs
          tests/order-payment-event-signed-webhook.test.mjs
          tests/order-payment-signed-authority-release.test.mjs
      - name: Prove Conversation and Message FORCE rollback in ephemeral PostgreSQL
        run: npm run audit:rls-conversation-message-force-rollback
        env:
          CONVERSATION_MESSAGE_FORCE_ROLLBACK_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - name: Prove FORCE-hardened Conversation and Message authority in ephemeral PostgreSQL
        run: npm run audit:rls-conversation-message-recipient
        env:
          CONVERSATION_MESSAGE_RECIPIENT_RLS_PROOF_DATABASE_URL: ${{ env.DIRECT_URL }}
      - run: npx tsc --noEmit
      - name: Lint
        run: npm run lint
      - name: Tests
        run: npm test
      - name: Security audit
        run: npm run audit:dependencies
      - name: Production build
        run: npm run build
